# 项目初始化 Prompt：SYSU Student Voice 举报/反映材料生成器

## 当前实现补充要求

当前 MVP 只需要实现“宿舍调整”一个场景，不再保留停车困难、道路破损、课程安排不合理等问题类型切换。

反映对象不要只做成一个下拉框，应以展开列表形式列出一批可考虑的对象类型。具体公开渠道、邮箱、网页和事实材料后续再补充；当前实现可以先保留占位清单，但不得编造未核实的具体联系方式。

证据材料结构暂时保持现状，后续再补充真实且已脱敏的材料。

## 一、项目目标

请初始化并实现一个名为 `SYSU-student-voice` 的前端静态网站项目。

项目核心目标是：

> 基于仓库中整理的事实、证据、诉求和反映渠道，一键生成举报信、情况反映、申诉材料或信息公开申请文本，并在生成后进行合规检查，帮助用户以真实、克制、合规、理性的方式表达个人诉求。

本项目不是自动群发工具，不是攻击平台，不是舆论煽动工具。  
本项目应定位为：

> 学生诉求材料生成与合规表达工具。

## 二、最高优先级要求

1. 所有文本生成必须基于仓库内结构化材料，不得凭空编造事实。
2. 所有生成内容必须尽量使用中性、正式、程序性的表达。
3. 项目不得鼓励侮辱、诽谤、造谣、人肉搜索、公开隐私、冒名发送、批量骚扰。
4. 不做后台自动发信。
5. 不做批量群发。
6. 不保存用户填写内容。
7. 所有逻辑尽量在浏览器本地完成。
8. 发送功能只能做成“打开邮件草稿”，用户必须自行审阅后发送。
9. UI 和 README 中都要强调：用户应自行核对事实、证据、措辞和个人信息，并对提交内容负责。
10. 所有材料、截图、图片都必须提示脱敏后再提交。

## 三、技术栈

- Vite
- Vanilla JavaScript
- HTML
- CSS

项目应能通过以下命令运行：

```bash
npm install
npm run dev
```

并能通过：

```bash
npm run build
```

生成静态站点。

## 四、目录结构

```text
SYSU-student-voice/
├─ README.md
├─ package.json
├─ index.html
├─ src/
│  ├─ main.js
│  ├─ style.css
│  ├─ generator/
│  │  ├─ generateLetter.js
│  │  ├─ renderTemplate.js
│  │  ├─ buildEvidenceText.js
│  │  └─ buildDemandsText.js
│  ├─ compliance/
│  │  ├─ checkText.js
│  │  └─ riskTerms.js
│  ├─ actions/
│  │  ├─ copyText.js
│  │  ├─ downloadText.js
│  │  └─ openMailDraft.js
│  └─ data/
│     └─ loadRepoData.js
├─ data/
│  ├─ cases.json
│  ├─ facts.json
│  ├─ evidence.json
│  ├─ demands.json
│  ├─ targets.json
│  └─ risk-terms.json
├─ templates/
│  ├─ report-letter.md
│  ├─ situation-report.md
│  ├─ appeal-letter.md
│  └─ disclosure-request.md
├─ public/
│  └─ materials/
│     ├─ notices/
│     ├─ screenshots/
│     ├─ photos/
│     └─ documents/
└─ docs/
   ├─ compliance.md
   ├─ redaction-guide.md
   └─ submit-material-guide.md
```

## 五、页面功能

请实现一个单页面 MVP。页面左侧是表单，右侧是生成结果预览。

左侧表单包含：问题类型选择、反映对象选择、材料类型选择、事实多选、证据多选、诉求多选、反映人姓名、联系方式、个人受到的具体影响、补充说明，以及生成材料、合规检查、复制正文、下载 Markdown、打开邮件草稿按钮。

右侧预览包含：生成后的正式文本、合规检查结果、风险提示和用户提交前确认提示。

## 六、数据文件格式

请创建 `data/cases.json`、`data/facts.json`、`data/evidence.json`、`data/demands.json`、`data/targets.json`、`data/risk-terms.json` 示例数据。示例内容应覆盖宿舍调整、停车困难、道路破损、课程安排不合理等问题，并包含宿舍调整相关事实、证据、诉求和反映对象。

## 七、模板文件

请创建 `templates/report-letter.md`、`templates/situation-report.md`、`templates/appeal-letter.md`、`templates/disclosure-request.md`。

模板应包含标题、收件对象、基本情况、相关材料、主要问题、请求事项、真实性与合规声明、反映人、联系方式和日期。信息公开申请模板应重点请求公开制度依据、决策程序、适用范围、执行标准、复核渠道和特殊困难处理机制。

## 八、核心代码要求

请实现模板替换、证据文本生成、诉求文本生成、材料生成、合规检查、复制、下载 Markdown、打开邮件草稿等模块。生成逻辑必须基于仓库内 JSON 数据和用户本地输入，不保存、不上传用户输入。

## 九、UI 文案要求

页面标题：

```text
SYSU Student Voice
举报/反映材料生成器
```

副标题：

```text
基于公开材料和个人经历生成正式文本。真实、克制、合规、理性表达。
```

按钮文案使用：

```text
生成材料
合规检查
复制正文
下载 Markdown
打开邮件草稿
```

不要使用：

```text
一键轰炸
一键开盒
一键举报学校
群发投诉
曝光个人
挂人
```

页面底部必须显示：

```text
本工具生成的是举报/反映材料草稿，不自动发送。用户应在提交前自行核对事实、证据、措辞和个人信息，并对提交内容负责。本工具不鼓励捏造事实、侮辱诽谤、公开隐私、冒名署名或批量骚扰。
```

## 十、README 要求

请创建完整 `README.md`，包含项目简介、项目目标、项目不做什么、功能列表、快速开始、目录结构、如何添加事实材料、如何添加证据材料、如何添加反映渠道、合规与隐私原则、贡献指南和免责声明。

README 中必须强调：本项目不自动发送邮件、不保存用户填写内容、不支持造谣、侮辱、诽谤、人肉、公开隐私、冒名、批量骚扰；用户对自己提交的内容负责；材料提交必须先脱敏。

## 十一、文档要求

`docs/compliance.md` 说明为什么要使用中性表达、哪些词属于高风险词、哪些事实需要证据、为什么不能公开个人隐私、为什么不能冒名或代表未经授权的人、为什么不做自动群发。

`docs/redaction-guide.md` 说明如何脱敏，必须覆盖姓名、学号、手机号、身份证号、宿舍号、门牌号、微信号、QQ号、邮箱、人脸、车牌、聊天头像、聊天昵称、私人联系方式。

`docs/submit-material-guide.md` 说明欢迎提交官方通知截图、公开网页链接、已脱敏聊天记录、公共区域照片、个人经历描述、渠道补充、模板建议、事实纠错、合规风险提醒；不接受未脱敏材料、侮辱性内容、无证据严重指控、私人联系方式、人肉信息、鼓励骚扰或刷屏的内容。

## 十二、实现验收标准

1. `npm install` 成功。
2. `npm run dev` 可以启动页面。
3. 页面可以从 JSON 加载案例、事实、证据、诉求和反映对象。
4. 用户选择后可以生成一份正式文本。
5. 生成文本包含标题、收件对象、基本情况、相关材料、主要问题、请求事项、真实性与合规声明、反映人和日期。
6. 合规检查能提示高风险词、需要证据支撑的表达、疑似个人信息。
7. 支持复制正文。
8. 支持下载 Markdown。
9. 支持打开邮件草稿。
10. 页面底部显示合规声明。
11. README 和 docs 文档完整。
12. 不存在后台发送、批量群发、用户数据保存功能。

## 十三、开发风格

UI 要求：简洁、清晰、严肃、不要过度煽动、不要做成攻击性视觉、使用绿色作为辅助色但不要使用真实校徽、校名标识或官方视觉资产、表单区和预览区并排布局、移动端也能基本使用。

代码要求：模块化、注释清晰、不写复杂依赖、不引入后端、不引入第三方发信服务、不引入用户追踪分析、不上传用户输入。

## 十四、最终输出

请直接完成项目初始化和 MVP 实现。提交时请给出已创建的文件列表、如何运行、已实现功能和后续建议。
